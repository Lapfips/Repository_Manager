#!/bin/bash

for repo in $(ls Repository_Manager/src/.Update_Repositories/.Update*); do
    if [[ $repo != ".Update_All" ]]; then
        bash "Repository_Manager/src/.Update_Repositories/$repo" "$1" "$2" "$3"
    fi
done