#!/bin/bash

github="git@github.com:"
repositories=()

for repo in "${repositories_optional[@]}"; do
    echo "Updating $repo"
    bash Repository_Manager/.Update/.UpdateRepo "$repo" "$github" "$1"
done