#!/bin/bash

github="git@github.com:"
repositories=( )

for repo in "${repositories[@]}"; do
    echo "Updating $repo"
    bash Repository_Manager/src/.Update/.Update_Repo "$repo" "$github" "$1"
done
